🌍 🏘️ World Population Dashboard

1. Communicate with the API using CONCURRENT programming
2. Perform an analysis and store the result in a CSV file.
3. Use a framework for dashboards such as Plotly Dash

4. Add TYPE ANNOTATIONS to the **classes** and **functions** you write

5. Use ITERATORS and GENERATORS to write a **data processing pipeline** that fits in nicely with the existing Python tooling
6. STORE the data locally in a database, use a CONTEXT MANAGER to manage **opening** and **closing** the database connection
7. Use LAMBDA functions to **define** and **apply** simple filters on your data.
